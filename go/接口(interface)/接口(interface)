定义
type Namer interface {
    Method1(param_list) return_type
    Method2(param_list) return_type
    ......
}

接口用于定义一组行为，其中每个行为由一个方法声明表示

namer是定义的接口类型
按照约定,(只包含一个方法的)接口的名字由方法名加[e]r后缀组成
当er后缀不合适时，以able结尾，或以I开头
